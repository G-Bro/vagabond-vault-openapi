get:
  summary: Get all player characters
  operationId: listPlayerCharacters
  tags:
    - Player Character
  responses:
    '200':
      description: A list of player characters
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/PlayerCharacter.yaml